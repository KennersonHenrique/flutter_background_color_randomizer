import 'package:flutter/material.dart';

import 'background_color_randomizer.dart';

void main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        body: BackgroundColorRandomizer(),
      ),
    ),
  );
}
